<?php
namespace App\Tools;

class ModelConstraint
{
    /**
     * Allows to check a row with a specified value already exists in a column A
     * inside a table B in a database C
     *
     * @param array $hashOptions mapping tables options
     * @return boolean
     */
    public static function alreadyColumnExists(array $hashOptions = array())
    {
        // TODO
        return false;
    }

    /**
     * Allows to check a row with a specified already exists in a column A
     * inside a table B in a database C
     *
     * @param array $hashOptions mapping tables options
     * @return boolean
     */
    public static function alreadyPairColumnExists(array $hashOptions = array())
    {
        // TODO
        return false;
    }
}
